[pytest]
asyncio_mode = auto
markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (requires running PostGIS + API)
    performance: Performance/SLA tests
    acceptance: Acceptance tests against 5 canonical sites
